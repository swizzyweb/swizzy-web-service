import { Response } from "express";

/**
 * Extended Express `Response` for Swizzy web services.
 * Currently equivalent to the base Express `Response`; extend as needed.
 */
export interface SwizzyResponse extends Response {}
