kind: pipeline
type: docker
name: test

steps:
  - name: install dependencies
    image: node:20
    commands:
      - npm config set @swizzyweb:registry http://bigun-1.swizzyweb.com:4783
      - npm install

  - name: run tests
    image: node:20
    commands:
      - npm run test
